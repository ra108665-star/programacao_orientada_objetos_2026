info:
  name: get
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/pedido
  body:
    type: json
    data: |-
      {
        "id": 1,
        "nome": "coxinha"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​